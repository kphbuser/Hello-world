pipeline {
	agent any
	tools {
		maven 'maven2'
	}
	stages {
		stage('Build') {
			steps {
				git "https://github.com/kphbuser/Hello-world.git"
			}
		}
		stage('Maven-Build') {
			steps {
				sh 'mvn clean package -DskipTests'
			}
		}

	}
}